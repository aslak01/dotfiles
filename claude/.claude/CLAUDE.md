# Global Claude Code instructions

Applies to every project on this machine. Symlinked from the dotfiles repo
(`claude/.claude/CLAUDE.md`) via `make`, so edits here are git-synced.

## Git

- Never add a `Co-Authored-By: Claude <...>` trailer — or any other Claude /
  Anthropic co-author line — to commits. Author commits as the user alone.
